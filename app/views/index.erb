<h1>Biblio</h1>

<h4>Log In Below:</h4>

<form action="/login" method="post">
    Username:
      <input type="text" name="username" id="username">

      <% if @user && !@user.errors.messages[:username].nil? %>
        <%= @user.errors.messages[:username].join("") %>
      <% end %><br>
    Password:
      <input type="text" name="password" id="password">
      
      <% if @user && !@user.errors.messages[:password].nil? %>
          <% @user.errors.messages[:password].each do |password_error| %>
            <%= password_error %>
        <% end %>
      <% end %><br>
    <input type="submit" value="Log In" id="submit">
</form>

<h4>Don't have an account? Signup <a href="/signup">here</a></h4>
